<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "BoxNet" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <!-- Font Awesome and Bootstrap -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" integrity="sha512-..." crossorigin="anonymous" referrerpolicy="no-referrer" />

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!--<nav class="navbar">
      <div class="col-md-2">
          <%= image_tag "logo_no_background.png", alt: "Website Logo" %>
      </div>
      <div class="col-md-10">

      </div>
    </nav>-->
    <nav class="navbar navbar-expand-md">
      <%= link_to root_path, class: "navbar-brand" do %>
        <%= image_tag "logo_w_fix_no_bg.png", alt: "Website Logo", height: 100, width: 185 %>
      <% end %>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
        <i class="fas fa-bars text-white"></i>
      </button>

      <div class="collapse navbar-collapse" id="navbarContent">
        <div class="navbar-nav w-100 justify-content-between align-items-center">
          <%= link_to "Results", results_path, class: "navbar-button" %>
          <%= link_to "Fixtures", fixtures_path, class: "navbar-button" %>
          <%= link_to "Boxers", "#", class: "navbar-button" %>
          <%= link_to "Rankings", "#", class: "navbar-button" %>

          <% if boxer_signed_in? %>
            <!--<span class="navbar-link">Boxer | <%= current_boxer.username %></span>-->
            <%= link_to current_boxer.full_name, boxer_profile_path(current_boxer.id), class: "navbar-button account" %>
            <%= link_to "Sign Out", destroy_boxer_session_path, method: :delete, class: "navbar-button in_out-btn" %>
          
          <% elsif coach_signed_in? %>
            <!--<span class="navbar-button account">Coach | <%= current_coach.username %></span>-->
            <%= link_to current_coach.full_name, coach_profile_path, class: "navbar-button account" %>
            <%= link_to "Sign Out", destroy_coach_session_path, method: :delete, class: "navbar-button in_out-btn" %>
          
          <% elsif spectator_signed_in? %>
            <span class="navbar-link">Spectator | <%= current_spectator.username %></span>
            <%= link_to "Sign Out", destroy_spectator_session_path, method: :delete, class: "navbar-button in_out-btn" %>
            
          <% elsif editor_signed_in? %>
            <span class="navbar-link">Editor | <%= current_editor.username %></span>
            <%= link_to "Sign Out", destroy_editor_session_path, method: :delete, class: "navbar-button in_out-btn" %>
          
          <% else %>
            <div class="dropdown">
              <button class="navbar-button dropdown-toggle" id="signin-dropdown">Sign In</button>
              <div class="dropdown-content">
                <%= link_to "Boxer", new_boxer_session_path, class: "navbar-link login-options" %>
                <%= link_to "Coach", new_coach_session_path, class: "navbar-link login-options" %>
                <%= link_to "Editor", new_editor_session_path, class: "navbar-link login-options" %>
                <%= link_to "Spectator", new_spectator_session_path, class: "navbar-link login-options" %>
              </div>
            </div>
            <%= link_to "Sign Up", signup_path, class: "navbar-button in_out-btn" %>
          <% end %>
        </div>
      </div>
    </nav>
    <%= yield %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
